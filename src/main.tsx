import React from 'react';
import ReactDOM from 'react-dom/client';
import BasicExemple from './exemple/Basic';

ReactDOM.createRoot(document.getElementById('root')!).render(
    <React.StrictMode>
        <BasicExemple />
    </React.StrictMode>,
);
